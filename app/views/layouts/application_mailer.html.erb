<html>
  <body>
    <body>
    <h1>It's an email!<h1>
      <h4>You just wasted <%= @order.total_cents / 100.00 %></h4>
      <ul>
        <% @order.line_items.each do |item| %>
        <li><%= item.product.name %></li>
        <% end %>
      </ul>
    </body>
  </body>
</html>
